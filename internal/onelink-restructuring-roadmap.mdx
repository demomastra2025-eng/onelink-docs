---
title: Onelink Restructuring Roadmap
description: Detailed phased plan for making the Onelink runtime faster, more reliable, and easier to scale.
---

# Onelink Restructuring Roadmap

## Why This Plan Exists

The current One Link Cloud runtime is not a small CRUD app and should not be optimized like one.
It is a large Rails monolith with:

- multiple frontend entrypoints
- multiple queue profiles
- real-time messaging flows
- heavy integration traffic
- AI and media workloads
- an enterprise extension layer that materially affects runtime behavior

The right target is not "split everything into microservices."
The right target is:

- a strong modular monolith
- clean queue isolation by SLA
- predictable runtime tuning
- one clear frontend state model
- observability-first operations
- autoscaling based on real saturation signals

## Current State Summary

### Backend

- Rails 7 monolith with enterprise overlay loaded into the same runtime
- two web containers behind nginx for rolling restarts
- one main Sidekiq plus specialized WhatsApp and transcription workers
- shared Postgres and shared Redis
- partial tracing and partial APM wiring, but not a full application observability model

### Frontend

- Vue 3 dashboard inside the Rails app
- hybrid state architecture with both `Vuex` and `Pinia`
- large dashboard surface area and uneven state ownership
- bundle limits exist for widget and SDK, but not for the main dashboard runtime

### Reliability

- queue isolation exists in some high-noise areas but not end-to-end
- dead jobs and retry behavior need a stronger policy
- scaling knobs existed in code before, but some were not made explicit in production config
- autoscaling is still operationally manual because the production runtime is Compose-based

## Principles

Every step in this roadmap should preserve these rules:

- do not split deploy units before internal boundaries are clear
- do not scale blind; measure first
- do not let low-value async work steal capacity from user-visible flows
- do not allow frontend state to have two competing "sources of truth"
- do not add more integration logic without idempotency and retry discipline
- do not protect a domain invariant with application-level `find-or-create`, a queue, or a lock alone when the database can enforce it

### Conflict And Concurrency Standard

Any write path that may receive concurrent, duplicated, retried, or out-of-order input must use the strongest practical layered guarantee rather than relying on timing:

1. define a deterministic domain identity or idempotency key before writing
2. enforce that identity with a database unique constraint, compare-and-swap invariant, or equivalent storage-level guarantee
3. use atomic `upsert` or `create-or-find` semantics and handle the losing concurrent writer as a normal success path
4. serialize only the narrow per-key transaction that owns create-once side effects
5. make retries and event replay idempotent
6. publish external side effects after commit, using an outbox where delivery guarantees justify it
7. migrate existing data through measured, resumable batches and build large indexes concurrently

Application locks remain useful for ordering compound side effects, but they are not the final correctness boundary. Queue partitioning can reduce contention, but it does not replace storage-level uniqueness because API calls, retries, maintenance tasks, and future integrations may bypass the original queue. Exceptions to this standard must document why a storage invariant is impossible and how duplicate or conflicting state is detected and repaired.

Apply this standard progressively to identity reconciliation, inbound webhook deduplication, provider event ingestion, singleton aggregate creation, outbound echo reconciliation, scheduling reservations, payment transitions, automation execution, and any other domain where two valid workers can race to create or transition the same logical record.

## What Was Worth Doing Immediately

The first wave of low-risk operational improvements should focus on clarity and control:

- make Puma worker scaling explicit with `WEB_CONCURRENCY`
- enable structured Rails request logging with `LOGRAGE_ENABLED`
- make specialized worker concurrency explicit in production env
- make Redis pool and rack-attack runtime knobs explicit in production env
- keep DB timeout policy visible through env instead of relying on implicit defaults

These changes improve operability now without changing the product model.

## Phase 1: Observability Baseline

### Goal

Make the runtime measurable before any deeper restructuring.

### Work

- define SLOs for:
  - API latency
  - queue latency
  - background job failure rate
  - webhook processing delay
  - ActionCable health
- add consistent correlation from nginx request id to Rails logs to Sidekiq jobs
- enable and validate structured logs across web and worker services
- enable `pg_stat_statements`
- establish a "top slow SQL" workflow
- add dashboards for:
  - p50/p95/p99 endpoint latency
  - queue size and queue latency
  - retry and dead job counts by class
  - Redis errors and timeouts
  - Evolution API failure rate and reconnect rate

### Exit Criteria

- slow endpoints are rankable by impact
- slow SQL is visible without ad hoc shell work
- dead jobs are grouped by wrapped class
- every production incident can be traced end-to-end

## Phase 2: Queue Isolation And Idempotency

### Goal

Protect user-visible flows from noisy or long-running background work.

### Work

- classify every job into one of these classes:
  - realtime
  - user-visible async
  - integrations
  - bulk
  - maintenance
  - AI/media
- keep `critical` and `high` focused on truly interactive work only
- move integration-heavy and bulk jobs out of the main Sidekiq pool
- add explicit retry policy per class:
  - short bounded retries for realtime
  - limited exponential retries for integrations
  - discard or quarantine for malformed payloads
- standardize idempotency for:
  - inbound webhooks
  - history sync
  - outbound echo reconciliation
  - audio transcription requeues
- inventory race-prone write paths and record for each one:
  - deterministic domain identity
  - database-enforced invariant
  - atomic conflict handling
  - create-once side effects
  - replay and repair strategy
- define dead-letter handling:
  - auto-discard classes
  - operator review classes
  - safe replay classes

### Exit Criteria

- low-value jobs cannot starve interactive jobs
- noisy integrations do not flood main worker capacity
- dead jobs are actionable instead of being a silent graveyard

## Phase 3: Web Runtime Hardening

### Goal

Make the web runtime predictable under burst load.

### Work

- standardize Puma math:
  - `WEB_CONCURRENCY`
  - `RAILS_MAX_THREADS`
  - `DB_POOL`
  - memory budget per process
- validate DB connection ceilings against total replica count
- separate ActionCable-sensitive traffic from ordinary HTTP capacity planning
- introduce graceful draining rules for web restarts and rolling deploys
- formalize gateway reload behavior after web container IP changes
- reduce long-tail request times by auditing:
  - inbox fetches
  - conversation list endpoints
  - typing and presence endpoints
  - heavy settings endpoints

### Exit Criteria

- p95 latency stays stable during deploys
- web scaling is capacity-driven, not guess-driven
- connection exhaustion is no longer a hidden scaling risk

## Phase 4: Backend Modular Monolith

### Goal

Keep one deployable application, but make internal domains explicit and separable.

### Target Contexts

- communication core
- CRM
- scheduling
- Captain and AI runtime
- automation and integrations
- WhatsApp and Evolution boundary
- portal and knowledge

### Work

- document ownership and boundaries for each context
- reduce cross-context callbacks and side effects
- move external provider logic behind adapters
- convert multi-step flows to command or orchestration services
- keep write paths explicit and idempotent
- route aggregate creation through domain-owned resolvers backed by database invariants
- move "mixed concern" services out of generic folders into domain-owned namespaces

### Communication Conversation Identity Reference Implementation

Use Conversation creation as the first full implementation of the conflict and concurrency standard:

- add an explicit deterministic Conversation identity key
- use `primary` for the single persistent Conversation of non-email realtime channels
- use the normalized provider/RFC email thread identity for each Email Conversation
- enforce uniqueness for `account + inbox + contact + conversation identity key` in PostgreSQL
- resolve concurrent creation through one domain service with atomic create-or-find behavior
- keep calls separated by `CallSession` and voice-message identity inside the persistent telephony Conversation
- keep create-once thread, assignment, CRM, automation, and notification side effects inside the controlled transaction boundary or after-commit delivery path
- backfill canonical keys in batches, preserve unresolved historical duplicates as explicit legacy records, and merge or repair them only through audited maintenance workflows
- retire `lock_to_single_conversation` as a mutable product setting after all creation paths use the identity strategy

#### Release A Implementation Status

Release A is implemented, independently reviewed, and present on `origin/aset/dev` as part of merge commit `ead8cf6b2a`. This status records source-control ancestry only; DEV and production rollout require separate runtime evidence and are not inferred from the branch:

- `conversations.identity_key` is introduced as nullable with a non-unique partial lookup index so rolling old writers remain compatible; the migration is resumable after a partial column/index failure
- `Conversations::IdentityResolver` owns `primary` identity resolution for non-email realtime channels, locks the current `ContactInbox`, serializes first creation with a deterministic PostgreSQL advisory lock, claims canonical legacy records, and atomically repairs stale source references plus their transport attributes
- SMS, telephony, WhatsApp, Telegram, Web Widget, API/social message builders, campaigns, reminders, and lead forms route persistent Conversation creation through the resolver
- Email remains on its existing multi-conversation thread contract during this expand phase
- native SIP calls remain separate `CallSession` lifecycles and Twilio calls keep immutable voice-message identities inside one persistent telephony Conversation; current-call markers and call-specific conference names prevent late events from one call overwriting another
- `ContactMergeAction` acquires the same ordered `ContactInbox` locks before moving Conversations, so a concurrent resolver resumes against the surviving Contact instead of creating a stale aggregate
- `Conversations::IdentityAudit` and `conversations:identity:audit` provide a read-only inventory of historical conflicts and dependent records before cleanup
- the current local artifact passed a 273-example identity/channel/provider RSpec matrix, a 434-example Voice/telephony matrix, 73 focused frontend call-lifecycle tests, scoped RuboCop/ESLint checks, and resumable migration lifecycle checks

Release B remains blocked on production audit evidence and will perform the controlled historical repair/backfill, including conflicts exposed by historical Contact merges, add the final database uniqueness constraint, and activate deterministic Email thread keys. The unique constraint must not be added before unresolved historical conflicts have been classified and repaired.

### Exit Criteria

- core domains are visible in code layout and service contracts
- integration logic does not leak into unrelated core entities
- future extraction becomes optional, not urgent
- domain identities and conflict guarantees are explicit instead of being distributed across provider-specific builders

## Phase 5: Frontend Simplification

### Goal

Give the dashboard one coherent state and loading model.

### Work

- freeze new `Vuex` module creation
- define `Pinia` as the destination state layer
- migrate bounded vertical slices atomically; `Vuex` and `Pinia` may coexist only for distinct slices, never as dual-read or dual-write owners of the same state
- migrate the highest-churn domains first:
  - conversation pagination
  - conversation search
  - inboxes
  - conversations
  - notifications
  - contact details and sidebars
- add dashboard bundle budgets in CI
- audit route-level chunking and heavy imports
- remove duplicated fetching patterns and mixed ownership between route, store, and component

#### Phase 5A Implementation Status

The first vertical slice is implemented, independently reviewed, and present on `origin/aset/dev`:

- commit `a86e152426` moves conversation pagination, end-of-list state, and per-scope totals from the legacy `conversationPage` Vuex module to one canonical Pinia store
- the dashboard entrypoint, conversation action helpers, and `ChatList` share the same Pinia instance; the old Vuex module, mutation types, dispatches, and dedicated Vuex specs are removed without a compatibility dual-write period
- a regression guard rejects new dashboard Vuex registry imports while allowing existing legacy modules to be removed
- the focused conversation/store matrix passed 413 examples, the final guard matrix passed 19 examples, scoped ESLint passed, and two exact-diff reviews returned `GO`
- commit `c99f3f7cc2` removes orphan employee Copilot wiring restored by the runtime reconciliation merge after the referenced components and helper had already been deleted; route tests passed 4 examples and the full production Vite build completed successfully
- neither commit is production deployment evidence; runtime promotion remains a separate controlled step

#### Phase 5B Implementation Status

The second vertical slice is implemented, independently reviewed, and present on `origin/aset/dev`:

- commit `600a2adf96` moves quick search, full search, and paginated contact, conversation, message, and article results from the legacy `conversationSearch` Vuex module to one canonical Pinia store
- `SearchView` reads Pinia refs and invokes Pinia actions directly; the Vuex registration, mutation constants, module, and dedicated Vuex specs are removed in the same atomic cutover
- the store preserves one shared abort signal across a full search, generation-based stale-response rejection across new searches and clears, append semantics for load-more requests, and controller isolation between store instances
- focused state/search verification passed 47 examples, scoped ESLint passed with one pre-existing dynamic-i18n warning, the production Vite build completed successfully, and the exact-diff review returned `GO`
- the broader state/search matrix passed 1569 of 1571 examples; the call timeout passed 71 of 71 examples when rerun in isolation, while the inbox stale-route test reproduced its `resolveInboxes is not a function` failure on the clean parent commit and remains an unrelated baseline defect
- this commit is source-control evidence only and does not claim DEV or production rollout

Phase 5C should migrate `inboxes` as the next bounded slice. Before that cutover, restore a green inbox action-test baseline through a root-cause fix for the clean-parent stale-route failure, then preserve account-switch stale-response protection as an explicit Pinia invariant.

### Exit Criteria

- new features land only in the target state model
- dashboard performance budgets are enforced automatically
- component reasoning does not require switching between multiple state paradigms

## Phase 6: Data And Cache Discipline

### Goal

Make shared state safe under horizontal scale.

### Work

- decide which runtime caches must be shared and which may stay process-local
- audit all `Rails.cache` usage for replica safety
- introduce PgBouncer if connection pressure requires it
- review Redis role separation:
  - Sidekiq
  - ActionCable
  - throttling
  - distributed locks
  - app cache
- keep timeouts, queue throughput, and cache TTL policy documented and observable
- measure database invariant migrations through batch latency, WAL volume, lock waits, dead tuples, and replica lag before increasing batch size

### Exit Criteria

- horizontally scaled web nodes behave consistently
- cache semantics are documented instead of accidental
- Redis contention is measurable by workload type
- concurrent writers converge on one canonical domain record without duplicate side effects

## Phase 7: Autoscaling And Platform Maturity

### Goal

Move from manual scaling to controlled self-scaling.

### Work

- choose an orchestration target that supports:
  - replica scaling
  - health-based rollout
  - queue-aware worker scaling
  - graceful termination
  - metrics-driven policy
- define autoscaling triggers for:
  - web CPU and p95 latency
  - queue depth and queue latency
  - webhook backlog
  - AI/media queue duration
- keep evolution and external integration capacity isolated from core web capacity

### Exit Criteria

- the platform can add or remove capacity without manual traffic juggling
- queue backlogs can trigger worker expansion without affecting web latency

## 30 / 60 / 90 Day View

### First 30 Days

- finish observability baseline
- classify all jobs by SLA
- define dead job policy
- standardize production runtime knobs
- identify top 10 slow endpoints and top 10 failing job classes

### Days 31-60

- split Sidekiq pools by workload class
- start frontend state migration plan
- document backend bounded contexts
- reduce long-tail latency in the highest-impact HTTP paths

### Days 61-90

- formalize shared-cache policy
- introduce queue-aware scaling model
- choose the post-Compose orchestration direction
- complete first frontend migration wave

## Non-Goals

These are explicitly not the first move:

- immediate microservice extraction
- rewriting the frontend from scratch
- changing every queue at once
- replacing Rails as the product core

## Success Metrics

- lower p95 and p99 API latency
- lower dead job inflow per day
- lower retry volume for integration-heavy queues
- predictable deploys without upstream churn
- smaller dashboard bundles and fewer duplicated requests
- ability to add capacity with explicit rules instead of operator guesswork
